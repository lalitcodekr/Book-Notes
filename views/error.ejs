<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error - Book Notes App</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body>
    <%- include('./partials/header') %>

    <div class="min-h-screen bg-gray-100 flex items-center justify-center p-4">
      <div
        class="bg-white p-6 sm:p-8 rounded-lg shadow-md max-w-md w-full text-center"
      >
        <h1 class="text-3xl sm:text-4xl font-bold text-red-500 mb-3 sm:mb-4">
          Oops!
        </h1>
        <p class="text-lg sm:text-xl text-gray-700 mb-3 sm:mb-4">
          <%= error %>
        </p>
        <% if (message) { %>
        <p class="text-sm sm:text-base text-gray-600 mb-5 sm:mb-6">
          <%= message %>
        </p>
        <% } %>
        <a
          href="/"
          class="inline-block bg-blue-500 text-white px-5 sm:px-6 py-2 rounded-lg text-sm sm:text-base hover:bg-blue-600"
        >
          Back to Home
        </a>
      </div>
    </div>

    <%- include('./partials/footer') %>
  </body>
</html>
